import { IrcSocketOptions } from './models/irc-socket-options.model';

const connectionOptions: IrcSocketOptions = {
  host: 'copper.libera.chat',
  port: 6667,
  user: {
    nick: 'Ju57_4_fun'
  }
};

export default connectionOptions;
